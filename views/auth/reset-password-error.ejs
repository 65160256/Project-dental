<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dentistry - Reset Link Invalid</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f5f5;
    }

    .container {
      display: flex;
      height: 100vh;
      min-height: 600px;
    }

    .left {
      flex: 1;
      position: relative;
      overflow: hidden;
    }

    .left img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .left::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      z-index: -1;
    }

    .right {
      flex: 1;
      background-color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px 20px;
    }

    .content-box {
      width: 100%;
      max-width: 450px;
      text-align: center;
    }

    .brand {
      text-align: center;
      margin-bottom: 30px;
    }

    .brand h2 {
      font-size: 32px;
      font-weight: 600;
      color: #2c5aa0;
      margin-bottom: 8px;
    }

    .subtitle {
      font-size: 18px;
      color: #666;
      font-weight: 400;
    }

    .error-icon {
      font-size: 64px;
      color: #dc3545;
      margin-bottom: 30px;
      display: block;
    }

    .error-message {
      background: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 30px;
      font-size: 16px;
      line-height: 1.5;
    }

    .error-title {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 12px;
      color: #721c24;
    }

    .help-section {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 30px;
      text-align: left;
    }

    .help-section h4 {
      color: #495057;
      margin-bottom: 15px;
      font-size: 16px;
    }

    .help-section ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .help-section li {
      padding: 8px 0;
      border-bottom: 1px solid #dee2e6;
      color: #6c757d;
      font-size: 14px;
      position: relative;
      padding-left: 20px;
    }

    .help-section li:last-child {
      border-bottom: none;
    }

    .help-section li:before {
      content: "•";
      color: #6c757d;
      font-weight: bold;
      position: absolute;
      left: 0;
    }

    .action-buttons {
      display: flex;
      flex-direction: column;
      gap: 15px;
      margin-bottom: 30px;
    }

    .btn {
      padding: 14px 24px;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
      text-align: center;
    }

    .btn-primary {
      background-color: #4285f4;
      color: white;
    }

    .btn-primary:hover {
      background-color: #3367d6;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(66, 133, 244, 0.3);
    }

    .btn-outline {
      background: transparent;
      color: #4285f4;
      border: 2px solid #4285f4;
    }

    .btn-outline:hover {
      background: #4285f4;
      color: white;
    }

    .security-note {
      background: #fff3cd;
      border: 1px solid #ffeaa7;
      border-radius: 6px;
      padding: 15px;
      margin-bottom: 20px;
      font-size: 14px;
      color: #856404;
      text-align: left;
    }

    .security-note strong {
      display: block;
      margin-bottom: 8px;
      color: #856404;
    }

    .footer-links {
      text-align: center;
      margin-top: 20px;
    }

    .footer-links a {
      color: #4285f4;
      text-decoration: none;
      font-weight: 500;
      margin: 0 15px;
      font-size: 14px;
      transition: color 0.3s ease;
    }

    .footer-links a:hover {
      color: #3367d6;
      text-decoration: underline;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        height: auto;
        min-height: 100vh;
      }

      .left {
        height: 200px;
        flex: none;
      }

      .right {
        flex: 1;
        padding: 30px 20px;
      }

      .content-box {
        max-width: 100%;
      }

      .brand h2 {
        font-size: 28px;
      }

      .footer-links a {
        display: block;
        margin: 5px 0;
      }
    }

    @media (max-width: 480px) {
      .right {
        padding: 20px 15px;
      }

      .action-buttons {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="left">
      <img src="/img/dentist.jpg" alt="Dental clinic interior" />
    </div>
    
    <div class="right">
      <div class="content-box">
        <div class="brand">
          <h2>Dentistry</h2>
          <div class="subtitle">Reset Password</div>
        </div>

        <div class="error-icon">
          <i class="fas fa-exclamation-triangle"></i>
        </div>

        <div class="error-message">
          <div class="error-title">This password reset link is invalid or has expired</div>
          <%= message %>
        </div>

        <div class="help-section">
          <h4>What happened?</h4>
          <ul>
            <li>The reset link may have expired (links are valid for 1 hour)</li>
            <li>The link may have already been used</li>
            <li>The link may have been copied incorrectly</li>
            <li>You may have requested a new reset link (old links become invalid)</li>
          </ul>
        </div>

        <div class="security-note">
          <strong>Security Information:</strong>
          For your security, password reset links expire quickly and can only be used once. This helps protect your account from unauthorized access.
        </div>

        <div class="action-buttons">
          <a href="/forgot-password" class="btn btn-primary">
            <i class="fas fa-redo"></i> Request a New Reset Link
          </a>
          
          <a href="/login" class="btn btn-outline">
            <i class="fas fa-arrow-left"></i> Back to Login
          </a>
        </div>

        <div class="footer-links">
          <a href="/login">Login</a>
          <a href="/register">Sign Up</a>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Log this event for security monitoring
    console.log('Invalid password reset attempt detected');
    
    // Track attempts (could be sent to analytics)
    if (typeof gtag !== 'undefined') {
      gtag('event', 'password_reset_invalid', {
        'event_category': 'security',
        'event_label': 'invalid_token'
      });
    }

    // Auto-focus on primary action button
    document.addEventListener('DOMContentLoaded', function() {
      const primaryBtn = document.querySelector('.btn-primary');
      if (primaryBtn) {
        primaryBtn.focus();
      }
    });

    // Prevent back button to insecure state
    window.history.replaceState(null, null, window.location.href);
    window.onpopstate = function() {
      window.history.replaceState(null, null, window.location.href);
    };
  </script>
</body>
</html>