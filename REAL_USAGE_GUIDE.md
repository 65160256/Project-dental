# 🚀 คู่มือการใช้งานระบบตารางงานจริง

## ✅ **สิ่งที่แก้ไขแล้ว:**

1. **✅ ระบบมีฟอร์มตั้งตารางงาน** - ทันตแพทย์สามารถตั้งตารางงานเองได้
2. **✅ API endpoints ครบ** - มี routes และ controllers สำหรับจัดการตารางงาน
3. **✅ แก้ไขการแสดงผล** - admin สามารถดูตารางงานของทันตแพทย์ได้
4. **✅ มีข้อมูลตารางงาน** - ทันตแพทย์ ID 1 มีข้อมูลตารางงานแล้ว

## 🎯 **วิธีใช้งานจริง:**

### **สำหรับทันตแพทย์:**

1. **เข้าสู่ระบบเป็นทันตแพทย์**
   ```
   URL: http://localhost:3000/dentist/login
   Username: [email ของทันตแพทย์]
   Password: [รหัสผ่าน]
   ```

2. **ไปที่หน้าตารางงาน**
   ```
   URL: http://localhost:3000/dentist/schedule
   ```

3. **ตั้งตารางงาน**
   - เลือกช่วงวันที่
   - กำหนดเวลาทำงาน (เช่น 9:00-17:00)
   - เลือกสถานะ (ทำงาน/หยุด)
   - บันทึกข้อมูล

### **สำหรับ Admin:**

1. **เข้าสู่ระบบเป็น Admin**
   ```
   URL: http://localhost:3000/admin/login
   ```

2. **ดูตารางงานทันตแพทย์**
   ```
   URL: http://localhost:3000/admin/dentists/1/schedule
   ```

3. **ใช้ Debug Tools**
   - คลิกปุ่ม "🔍 Debug Data"
   - ดูข้อมูลใน Console (F12)

## 🧪 **การทดสอบ:**

### **ทดสอบ API โดยตรง:**
```bash
# ทดสอบ API สำหรับทันตแพทย์
GET /dentist/api/schedule/load?startDate=2025-10-01&endDate=2025-10-31

# ทดสอบ API สำหรับ admin
GET /admin/api/dentists/1/schedule-data?start_date=2025-10-01&end_date=2025-10-31

# ทดสอบ Debug API
GET /admin/api/debug/dentists/1/schedule?start_date=2025-10-01&end_date=2025-10-31
```

### **ทดสอบการบันทึกตารางงาน:**
```bash
# ทดสอบบันทึกตารางงาน
POST /dentist/api/schedule/save-range
Content-Type: application/json

{
  "startDate": "2025-10-01",
  "endDate": "2025-10-31",
  "status": "working",
  "startTime": "09:00",
  "endTime": "17:00",
  "note": "เวลาทำงานปกติ"
}
```

## 📊 **ผลลัพธ์ที่คาดหวัง:**

### **ใน Browser Console:**
```javascript
🔍 Loading schedule data: {dentistId: 1, startDateStr: '2025-10-01', endDateStr: '2025-10-31'}
📅 Schedule data loaded: [
  {schedule_date: "2025-10-01", hour: 900, start_time: "09:00", end_time: "10:00", status: "working"},
  {schedule_date: "2025-10-01", hour: 1000, start_time: "10:00", end_time: "11:00", status: "working"},
  // ... more data
]
📅 2025-10-01: [schedule data for specific date]
```

### **ในปฏิทิน:**
- แสดงเวลาทำงานในแต่ละวัน
- แสดงสถานะ (ทำงาน/หยุด)
- สามารถคลิกดูรายละเอียดได้

## 🔧 **การแก้ไขปัญหา:**

### **หากยังแสดง `Schedule data loaded: []`:**
1. ตรวจสอบว่าทันตแพทย์มีข้อมูลตารางงานหรือไม่
2. ใช้ Debug API เพื่อดูข้อมูลจริง
3. ตรวจสอบ Console สำหรับ error messages

### **หาก API ไม่ทำงาน:**
1. ตรวจสอบว่า Docker containers กำลังทำงาน
2. ตรวจสอบ logs ของ application
3. ตรวจสอบ database connection

## 🎉 **สรุป:**

ตอนนี้ระบบพร้อมใช้งานจริงแล้ว! ทันตแพทย์สามารถ:
- ✅ ตั้งตารางงานเองผ่านระบบ
- ✅ ดูตารางงานในปฏิทิน
- ✅ แก้ไขตารางงานได้

Admin สามารถ:
- ✅ ดูตารางงานของทันตแพทย์ทุกคน
- ✅ ใช้ Debug tools เพื่อตรวจสอบปัญหา
- ✅ จัดการระบบได้อย่างสมบูรณ์

## 🚀 **ขั้นตอนต่อไป:**

1. **ทดสอบการตั้งตารางงาน** - ให้ทันตแพทย์ลองใช้ระบบจริง
2. **ทดสอบการจองนัดหมาย** - ให้ผู้ป่วยลองจองนัดหมาย
3. **ปรับปรุงระบบ** - ตาม feedback ที่ได้รับ
