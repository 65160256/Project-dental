# 📅 คู่มือการตั้งตารางงานสำหรับทันตแพทย์

## 🎯 **ปัญหาที่พบ:**
ทันตแพทย์ ID 1 ไม่มีข้อมูลตารางงานในระบบ ทำให้:
- ปฏิทินแสดงผลเป็น array ว่าง `[]`
- ผู้ป่วยไม่สามารถจองนัดหมายได้
- ระบบไม่สามารถแสดงเวลาทำงานได้

## 🛠️ **วิธีแก้ไข:**

### **ตัวเลือกที่ 1: ให้ทันตแพทย์ตั้งตารางงานเอง**

1. **เข้าสู่ระบบเป็นทันตแพทย์**
   ```
   URL: http://localhost:3000/dentist/login
   ```

2. **ไปที่หน้าตารางงาน**
   ```
   URL: http://localhost:3000/dentist/schedule
   ```

3. **ตั้งตารางงาน**
   - เลือกวันที่
   - กำหนดเวลาทำงาน
   - บันทึกข้อมูล

### **ตัวเลือกที่ 2: Admin ตั้งตารางงานให้**

1. **เข้าสู่ระบบเป็น Admin**
   ```
   URL: http://localhost:3000/admin/login
   ```

2. **ไปที่หน้าจัดการทันตแพทย์**
   ```
   URL: http://localhost:3000/admin/dentists/1/schedule
   ```

3. **ตั้งตารางงานให้ทันตแพทย์**
   - เลือกวันที่
   - กำหนดเวลาทำงาน
   - บันทึกข้อมูล

### **ตัวเลือกที่ 3: สร้างข้อมูลตารางงานอัตโนมัติ**

```sql
-- สร้างข้อมูลตารางงานเริ่มต้นสำหรับทันตแพทย์ ID 1
-- ตั้งเวลาทำงาน 9:00-17:00 ทุกวันจันทร์-เสาร์

INSERT INTO dentist_schedule (dentist_id, schedule_date, day_of_week, hour, status, start_time, end_time, note)
SELECT 
    1 as dentist_id,
    DATE_ADD('2025-10-01', INTERVAL seq.seq DAY) as schedule_date,
    DAYOFWEEK(DATE_ADD('2025-10-01', INTERVAL seq.seq DAY)) as day_of_week,
    hour_seq.hour,
    'working' as status,
    CONCAT(hour_seq.hour, ':00:00') as start_time,
    CONCAT(hour_seq.hour + 1, ':00:00') as end_time,
    'เวลาทำงานปกติ' as note
FROM (
    SELECT 0 as seq UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10 UNION SELECT 11 UNION SELECT 12 UNION SELECT 13 UNION SELECT 14 UNION SELECT 15 UNION SELECT 16 UNION SELECT 17 UNION SELECT 18 UNION SELECT 19 UNION SELECT 20 UNION SELECT 21 UNION SELECT 22 UNION SELECT 23 UNION SELECT 24 UNION SELECT 25 UNION SELECT 26 UNION SELECT 27 UNION SELECT 28 UNION SELECT 29 UNION SELECT 30
) seq
CROSS JOIN (
    SELECT 9 as hour UNION SELECT 10 UNION SELECT 11 UNION SELECT 12 UNION SELECT 13 UNION SELECT 14 UNION SELECT 15 UNION SELECT 16
) hour_seq
WHERE DAYOFWEEK(DATE_ADD('2025-10-01', INTERVAL seq.seq DAY)) BETWEEN 2 AND 7  -- จันทร์-เสาร์
  AND DATE_ADD('2025-10-01', INTERVAL seq.seq DAY) <= '2025-10-31';
```

## 📊 **ผลลัพธ์ที่คาดหวัง:**

หลังจากตั้งตารางงานแล้ว ระบบควรแสดง:
- `Schedule data loaded: [array of schedule objects]` แทนที่จะเป็น `[]`
- ปฏิทินแสดงเวลาทำงาน
- ผู้ป่วยสามารถจองนัดหมายได้

## 🔍 **การตรวจสอบ:**

1. **ตรวจสอบข้อมูลในฐานข้อมูล:**
   ```sql
   SELECT COUNT(*) FROM dentist_schedule WHERE dentist_id = 1;
   ```

2. **ทดสอบ API:**
   ```
   GET /admin/api/dentists/1/schedule-data?start_date=2025-10-01&end_date=2025-10-31
   ```

3. **ทดสอบ Frontend:**
   - รีเฟรชหน้าเว็บ
   - ดู Console (F12)
   - ตรวจสอบข้อมูลที่แสดง

## 💡 **คำแนะนำ:**

**วิธีที่ดีที่สุด:** ให้ทันตแพทย์ตั้งตารางงานเองผ่านระบบ เพราะ:
- ทันตแพทย์รู้เวลาที่ตัวเองว่าง
- สามารถปรับเปลี่ยนได้ตามความเหมาะสม
- เป็นการฝึกใช้ระบบจริง
